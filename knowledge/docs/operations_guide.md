# Linux Operations Guide

For infrastructure operations, use controlled shell commands with timeouts and captured stdout, stderr, and exit code.

Log analysis workflows should retrieve relevant context, summarize repeated failures, and preserve technical detail in the final response.

Remote agent delegation is useful when a separate reporting or diagnostics agent has better domain specialization.
